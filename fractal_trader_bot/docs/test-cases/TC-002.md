# ТЕСТ-КЕЙС #002

---

## 1. Заголовок
Проверка глобальных риск-лимитов

---

## 2. ID
TC-002

## 3. Приоритет
P1 (High)

---

## 4. Предусловия
- RiskManager инициализирован с тестовым конфигом
- daily_loss_limit_abs = 10
- max_drawdown_percent = 15.0
- emergency_stop = false

---

## 5. Шаги

| # | Шаг | Ожидаемый результат |
|:---|:---|:---|
| 1 | Вызвать `can_trade()` при `emergency_stop=True` | `(False, "Аварийная остановка")` |
| 2 | Вызвать `can_trade()` при `pnl_today=-15` | `(False, "Дневной лимит убытка")` |
| 3 | Вызвать `can_trade()` при просадке > 15% | `(False, "Просадка ...%")` |
| 4 | Вызвать `can_trade()` с пустыми позициями и лимитами | `(True, "OK")` |
| 5 | Проверить `can_trade_symbol("SCAM-USDT")` с чёрным списком | `(False, "В черном списке")` |

---

## 6. Ожидаемый результат
Все риск-лимиты корректно блокируют торговлю при превышении.

---

## 7. Фактический результат (если баг)

---

## 8. Статус
**Статус:** Pass / Fail  
**Выполнил:** Константин Горбунов  
**Дата:** 2026-06-24
