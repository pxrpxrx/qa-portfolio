# ТЕСТ-КЕЙС #003

---

## 1. Заголовок
Запись и обновление позиции в БД

---

## 2. ID
TC-003

## 3. Приоритет
P0 (Blocker)

---

## 4. Предусловия
- BingXDBWriter инициализирован с in-memory SQLite
- Таблицы `positions` и `symbol_stats` созданы автоматически

---

## 5. Шаги

| # | Шаг | Ожидаемый результат |
|:---|:---|:---|
| 1 | Вызвать `add_position(symbol="BTC-USDT", side="BUY", entry_price=50000, quantity=0.001, stop_percent=0.5, take_percent=1.5)` | Возвращается ID > 0 |
| 2 | Вызвать `get_open_positions()` | Список содержит 1 запись с symbol="BTC-USDT" и status="OPEN" |
| 3 | Вызвать `update_position(symbol="BTC-USDT", exit_price=51000, realized_pnl=10, realized_pnl_percent=2.0, exit_reason="TAKE_PROFIT")` | Возвращается ID позиции |
| 4 | Вызвать `get_open_positions()` снова | Список пуст (позиция закрыта) |

---

## 6. Ожидаемый результат
Позиция успешно записана и затем закрыта, данные в БД консистентны.

---

## 7. Фактический результат (если баг)

---

## 8. Статус
**Статус:** Pass / Fail  
**Выполнил:** Константин Горбунов  
**Дата:** 2026-06-24
