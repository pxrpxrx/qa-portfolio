# БАГ-РЕПОРТ #002

---

## 1. Заголовок
Импорт bx_fractal и positionSizing отсутствуют в репозитории

---

## 2. Метка приоритета
**Приоритет:** P1 (High)  
**Серьезность:** Blocker

---

## 3. Описание
Два модуля, импортируемые основными компонентами бота, отсутствуют в репозитории:
- `bx_fractal.py` (импортируется в `bx_trail_traider.py:11`)
- `positionSizing.py` (импортируется в `bx_monitor.py:9`)

Попытка запуска `bx_trail_traider.py` или `bx_monitor.py` приводит к `ModuleNotFoundError`. Это блокирует как запуск бота, так и выполнение тестов без мокирования.

---

## 4. Шаги для воспроизведения
1. Склонировать репозиторий
2. Выполнить `python src/bx_trail_traider.py`
3. Выполнить `python src/services/bx_monitor.py`

---

## 5. Фактический результат
```
ModuleNotFoundError: No module named 'bx_fractal'
```
```
ModuleNotFoundError: No module named 'positionSizing'
```

---

## 6. Ожидаемый результат
Модули `bx_fractal.py` и `positionSizing.py` присутствуют в репозитории, либо их функциональность встроена в существующие модули.

---

## 7. Окружение
- Python 3.10+
- Файл: `src/bx_trail_traider.py:11`
- Файл: `src/services/bx_monitor.py:9`

---

## 8. Предполагаемая причина
Модули не были добавлены в репозиторий при коммите. Возможно, они находятся в разработке или были случайно исключены из `.gitignore`.

---

## 9. Ссылки
- `src/bx_trail_traider.py:11` — строка импорта `from bx_fractal import FractalFinder`
- `src/services/bx_monitor.py:9` — строка импорта `from positionSizing import positionSizing`

---

## 10. Статус
**Статус:** Open  
**Создал:** QA Engineer  
**Дата:** 2025-06-24
